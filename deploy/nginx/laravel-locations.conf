# Shared Laravel routes (included by each server block)

location ^~ /lunar {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /api {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /livewire {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /storage {
    expires 30d;
    add_header Cache-Control "public, max-age=2592000, immutable";
    access_log off;
    try_files $uri =404;
}

location ^~ /build {
    try_files $uri =404;
}

location ^~ /css/ {
    try_files $uri =404;
}

location ^~ /js/ {
    try_files $uri =404;
}

location ^~ /fonts/ {
    try_files $uri =404;
}

location = /up {
    try_files $uri /index.php?$query_string;
}

location = /sitemap.xml {
    try_files $uri /index.php?$query_string;
}

location = /robots.txt {
    try_files $uri /index.php?$query_string;
}

location ^~ /feed/ {
    try_files $uri /index.php?$query_string;
}

location ^~ /sepet {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /odeme {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /siparis {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /hesabim/siparis {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /sayfa {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /blog {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /koleksiyon {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /urun {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /ara {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /giris {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /kayit {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /hesabim {
    try_files $uri $uri/ /index.php?$query_string;
}

location ^~ /cikis {
    try_files $uri $uri/ /index.php?$query_string;
}
